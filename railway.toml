[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Health check for the bot webhook server
healthcheckPath = "/health"
healthcheckTimeout = 300

# Internal port the app listens on
numReplicas = 1
